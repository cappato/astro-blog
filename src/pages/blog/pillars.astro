---
/**
 * Página de Pilares de Contenido
 * 
 * Muestra todos los pilares de contenido organizados y con estadísticas
 */

import MainLayout from '../../layouts/MainLayout.astro';
import { PillarGrid } from '../../features/content-pillars/components';
import { getFeaturedPillars, getAllPillars } from '../../features/content-pillars/config/pillars.config';
import ContentContainer from '../../components/layout/ContentContainer.astro';
import ContentWidth from '../../components/layout/ContentWidth.astro';
import Card from '../../components/ui/Card.astro';

// Obtener pilares
const featuredPillars = getFeaturedPillars();
const allPillars = getAllPillars();

// SEO metadata
const seoData = {
  title: 'Pilares de Contenido - Organización Temática del Blog',
  description: 'Explora los pilares de contenido del blog: Astro & Performance, TypeScript & Architecture, Automation & DevOps, y SEO & Optimization.',
  keywords: ['pilares contenido', 'blog organización', 'astro', 'typescript', 'devops', 'seo'],
  type: 'website' as const
};
---

<MainLayout {...seoData}>
  <!-- Hero Section -->
  <ContentContainer as="section" className="py-8">
    <ContentWidth width="standard">
      <div class="text-center">
        <h1 class="text-4xl md:text-5xl font-bold text-blue-600 dark:text-blue-400 mb-6">
          Pilares de Contenido
        </h1>
        <p class="text-xl text-gray-600 dark:text-gray-300 leading-relaxed mb-8">
          Explora el contenido del blog organizado por temáticas estratégicas.
          Cada pilar representa un área de especialización con artículos profundos
          y recursos valiosos para desarrolladores.
        </p>
      </div>
    </ContentWidth>
  </ContentContainer>

  <!-- Featured Pillars -->
  {featuredPillars.length > 0 && (
    <ContentContainer as="section" className="py-8">
      <ContentWidth width="standard">
        <div class="mb-8">
          <h2 class="text-3xl font-bold text-blue-600 dark:text-blue-400 mb-4">
            🌟 Pilares Destacados
          </h2>
          <p class="text-gray-600 dark:text-gray-300 text-lg">
            Los pilares principales que definen la identidad y especialización del blog.
          </p>
        </div>

        <PillarGrid
          pillars={featuredPillars}
          variant="featured"
          columns={{ desktop: 2, tablet: 2, mobile: 1 }}
          showStats={true}
          className="featured-pillars"
        />
      </ContentWidth>
    </ContentContainer>
  )}

  <!-- All Pillars -->
  <ContentContainer as="section" className="py-8">
    <ContentWidth width="standard">
      <div class="mb-8">
        <h2 class="text-3xl font-bold text-blue-600 dark:text-blue-400 mb-4">
          📚 Todos los Pilares
        </h2>
        <p class="text-gray-600 dark:text-gray-300 text-lg">
          Explora todas las temáticas disponibles y encuentra el contenido que más te interese.
        </p>
      </div>

      <PillarGrid
        pillars={allPillars}
        variant="default"
        columns={{ desktop: 2, tablet: 2, mobile: 1 }}
        showStats={true}
        className="all-pillars"
      />
    </ContentWidth>
  </ContentContainer>

  <!-- CTA Section -->
  <ContentContainer as="section" className="py-12">
    <ContentWidth width="standard">
      <Card variant="default" className="text-center p-8">
        <h2 class="text-2xl font-bold text-blue-600 dark:text-blue-400 mb-4">
          ¿Buscas algo específico?
        </h2>
        <p class="text-gray-600 dark:text-gray-300 text-lg mb-6">
          Navega por todos los artículos del blog o busca por tags específicos
          para encontrar exactamente lo que necesitas.
        </p>
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <a
            href="/blog"
            class="btn btn-primary"
          >
            Ver Todos los Artículos
          </a>
          <a
            href="/blog/tags"
            class="btn btn-secondary"
          >
            Explorar por Tags
          </a>
        </div>
      </Card>
    </ContentWidth>
  </ContentContainer>
</MainLayout>

<!-- CSS eliminado - Usar Tailwind directo: mb-16, mb-8, inline-flex items-center justify-center px-8 py-3 rounded-lg font-semibold no-underline transition-all duration-200 border-2 border-transparent bg-blue-600 dark:bg-blue-500 text-white hover:bg-blue-600 dark:bg-blue-500-600 hover:-translate-y-0.5 hover:shadow-lg -->
