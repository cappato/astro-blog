---
/**
 * Componente ThemeScript - Inyecta el script anti-flicker en el <head>
 * Debe incluirse en todos los layouts para prevenir flashes de tema incorrecto
 */
import { getThemeInitScript } from '../../utils/theme.ts';
---

<!-- Script anti-flicker que se ejecuta inmediatamente -->
<script is:inline set:html={getThemeInitScript()}></script>
