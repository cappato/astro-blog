---
/**
 * TagButton - Wrapper para BaseTag (mantiene compatibilidad)
 * @deprecated Usar BaseTag directamente en nuevos componentes
 */
import BaseTag from './BaseTag.astro';

interface Props {
  tag: string;
}

const { tag } = Astro.props;
---

<BaseTag tag={tag} />


