---
interface Props {
  variant?: 'default' | 'hover' | 'compact';
  className?: string;
  as?: 'div' | 'article' | 'section';
}

const {
  variant = 'default',
  className = '',
  as: Element = 'div'
} = Astro.props;

// Definir las variantes de la card usando solo Tailwind - sin fondos específicos
const variants = {
  default: 'rounded-lg shadow-sm p-4 sm:p-6',
  hover: 'rounded-lg shadow-sm hover:shadow-md transition-shadow duration-200 p-4 sm:p-6',
  compact: 'rounded-lg shadow-sm p-3 sm:p-4'
};

const cardClasses = `${variants[variant as keyof typeof variants]} ${className}`;
---

<Element class={cardClasses}>
  <slot />
</Element>
